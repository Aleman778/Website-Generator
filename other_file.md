### Hello this text is included from another file

You can do this by putting `@include "other_file.md"` in the beginning of a line.
That will essentially dump the contents of the other file into the current one.